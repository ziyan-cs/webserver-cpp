# v0.1-Tcp echo server

## 阶段目标
实现单线程、阻塞IO的回声服务器

---

## 2026-05-15

### 实现进度
1. 确定项目采用 CMake 三层架构，多模块管理便于扩展、维护
2. 实现 Config 类：封装 port、ip、backlog 等服务器配置信息，
    - 支持默认初始化与自定义覆盖
3. 简单实现 Logger 日志宏：提供 INFO/DEBUG/WARN/ERROR 四级日志输出方法
4. 初步实现 Socket 类：实现服务端套接字创建、绑定、监听与 RAII 资源管理
    - 基于已有 阻塞IO 知识快速实现，为后续 epoll 非模型模型迭代做基础

### 遇到问题
1. 编译报错：ld: cannot find -lcore / -lhttp / -llog / -lpool
    - 同级模块 serer/main.cpp 正常，仅 server/TcpServer.h 模块链接失败
    - 头文件包含、CMake 语法均正确，但始终无法编译
    - 执行 git push 后，问题自动消失
 根因：CMake 构建缓存（build/ 目录）损坏，强制清理缓存 + 完全重新构建修复

 ### 后续计划
 1. 完成 阻塞IO 版本的 echo 服务器
 2. 学习 epoll，实现 net 与 core 模块间的回调逻辑以及分层管理

---

## 2026-05-16

### 实现进度

### 遇到问题
1. LOG_INFO() 打印客户端发来消息出错
    - 直接用 << buffer.data() 输出，导致乱码和越界问题
    - 前置 buffer[n] = '\0' 解决问题
 根因：recv() 收到的数据是二进制流，不会自动加上 \0 结尾